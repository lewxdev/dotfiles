# Application aliases

# Native macOS applications...

alias chrome="open ${DIR[APPS]}/Google\ Chrome.app"
alias logi="open ${DIR[APPS]}/Logi\ Options+.app"
alias slack="open ${DIR[APPS]}/Slack.app"
alias spotify="open ${DIR[APPS]}/Spotify.app"
alias zoom="open ${DIR[APPS]}/zoom.us.app"

# Chrome applications (Progressive Web Apps / Shortcuts)...

# Developer
alias devdocs="open ${DIR[PWA]}/DevDocs.app"
alias mui="open ${DIR[PWA]}/MUI.app"

# Google
alias calendar="open ${DIR[PWA]}/Google\ Calendar.app"
alias drive="open ${DIR[PWA]}/Google\ Drive.app"
alias gmail="open ${DIR[PWA]}/Gmail.app"
alias meet="open ${DIR[PWA]}/Google\ Meet.app"
alias messages="open ${DIR[PWA]}/Messages.app"
alias photos="open ${DIR[PWA]}/Google\ Photos.app"
alias youtube="open ${DIR[PWA]}/YouTube.app"

# Misc
alias soundcloud="open ${DIR[PWA]}/SoundCloud.app"
